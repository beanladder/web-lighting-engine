# LumenForge

A browser-based lighting engine: import a model, light it, bake it, export it.

Work in progress — being built up in small commits. Full docs land once the
editor is in place.

```bash
npm install
npm run dev
```
